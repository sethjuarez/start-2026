# Divya Notes
Hello! I think we could show below

1. Subset of agents are deployed to run locally on Windows.
2. They run in a session container (or another we have spectrum of isolation that MXC offers from process, session, Micro VM, VM, Linux containers etc.) based on the intent and risk of the task.
3. Show agents have access only to what you define (network, files etc.)
4. You can show asking to delete files on the desktop or files of another project in a different folder and will not work so you can highlight containment and limited resource access
5. Show these agents run with their own unique ID (Entra ID) and their actions can be observed and differentiated from user actions.
6. All of A365 observability and manageability flows to all agents running Windows.
7. Uber point - long running autonomous agents can be deployed on Windows securely and managed effectively, while helping you achieve token efficiency.

